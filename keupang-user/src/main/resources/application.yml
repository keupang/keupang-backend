spring:
    application:
        name: user
    profiles:
        active: dev
    config:
        import: optional:configserver:http://${security_username}:${security_password}@localhost:9000
eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
            defaultZone: http://${security_username}:${security_password}@localhost:8761/eureka
