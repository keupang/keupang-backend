spring:
    config:
        import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:9000}
    data:
        redis:
            host: ${REDIS_HOST:localhost}
            port: ${REDIS_PORT:6379}
